{
  "_comment": "Base config. Sensitive/environment-specific values (Cognito IDs, connection strings) are loaded from appsettings.Development.json in local dev and from environment variables / AWS Secrets Manager in staging and production. Never hardcode real values here.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "CacheSettings": {
    "InstancePrefix": "plantonhub:",
    "DefaultTtlMinutes": 5
  },
  "AntiFraudSettings": {
    "StaleEventThresholdHours": 48,
    "ClockSkewThresholdMinutes": 5,
    "MinimumAppVersion": "1.0.0",
    "ReplayAttackThreshold": 3,
    "ReplayAttackWindowMinutes": 10,
    "FaceMatchThreshold": 0.6
  },
  "Cors": {
    "AllowedOrigins": "http://localhost:3000,http://localhost:5173"
  }
}
